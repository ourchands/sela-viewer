- name: استنساخ Firestorm
  run: |
    git clone https://github.com/.../firestorm.git firestorm  # ← ننشئ المجلد أولاً!
    
- name: بناء Sela Viewer
  run: |
    cd firestorm
    ./autobuild build -c ReleaseOS  # ← نستخدم autobuild الموجود في firestorm
```

---

## 📝 خطوات التصحيح:

### 1. افتح ملف build.yml
- Code → .github → workflows → build.yml

### 2. اضغط Edit (القلم ✏️)

### 3. احذف **كل شيء** (Ctrl+A ثم Delete)

### 4. الصق الكود الجديد أعلاه (كله!)

### 5. احفظ
- رسالة: "إصلاح مشكلة البناء"
- اضغط "Commit changes"

---

## 🚀 بعد الحفظ:

### شغّل البناء من جديد:
1. Actions → بناء Sela Viewer
2. Run workflow
3. اضغط الزر الأخضر

---

## ⏰ هذه المرة يجب أن ينجح!

البناء سيمر بهذه المراحل:
```
✓ تحميل المستودع (10 ثانية)
✓ تثبيت المتطلبات (5-10 دقائق)
✓ استنساخ Firestorm (5-10 دقائق)
✓ نسخ ملفات العربية (1 دقيقة)
⏰ البناء (60-120 دقيقة) ← هنا سيأخذ وقت طويل
✓ رفع النتيجة (2-5 دقائق)
